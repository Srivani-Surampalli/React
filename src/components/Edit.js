import React from 'react';

const EditDashboard = (props) => {
    console.log(props);
        return (
            <div>
                This is Edit page content..pros are 
            </div>
        );
    };

export default EditDashboard;